services:
  mysql:
    image: mysql:8.4
    container_name: makcom_ads_mysql
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      MYSQL_DATABASE: makcom_ads
    ports:
      # exposed externally; root has a 32-char random password.
      # Restrict access via Aliyun security group / firewall to trusted IPs.
      - "33307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    container_name: makcom_ads_migrate
    env_file:
      - .env.production
    command: npx prisma migrate deploy
    depends_on:
      mysql:
        condition: service_healthy
    profiles:
      - migrate

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: makcom_ads_app
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env.production
    depends_on:
      mysql:
        condition: service_healthy


volumes:
  mysql_data:
